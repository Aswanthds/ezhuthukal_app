const baseUrl = "http://127.0.0.1:8000/api/";

const token = "${baseUrl}token/";
const refreshToken = "${baseUrl}token/refresh/";
const verifyToken = "${baseUrl}token/verify/";
const users = "${baseUrl}users/";
const comments = "${baseUrl}comments/";
const aadmin = "$baseUrl/admin/ ";

const registerUser = "${users}register/";

const currentUser = "${baseUrl}users/me";

const allUSers = "${users}allUsers/";

const deleteUser = "${users}deleteUser/";

const updateUser = "${users}updateUser/ ";

const articleList = "${baseUrl}articles/all/";
const articleAdd = "${baseUrl}articles/add/";
const deleteArticle = "${baseUrl}articles/delete/";
